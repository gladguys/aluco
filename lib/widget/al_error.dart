import 'package:flutter/material.dart';

class ALError extends StatelessWidget {
  const ALError();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('DEU MUITO RUIM'),
    );
  }
}
